#include <stdio.h>
void main() {
    int n = 56; 
    if(n%2 == 0) {
        printf("%d is even", n);
    } else {
        printf("%d is odd", n);
    }

    /**
     * is n divisiable by 7 and 8?
     */
    // if (n%7 == 0 && n%8 == 0) {
    //     printf("%d is divisiable by 7 and 8", n);
    // } else {
    //     printf("%d is not divisiable by 7 and 8", n);
    // }
} 