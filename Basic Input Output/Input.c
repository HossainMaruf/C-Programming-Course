#include <stdio.h>
void main() {
    // int a = 2, b = 3;
    int a, b;
    scanf("%d%d", &a, &b); // for keyboard input
    // scanf("%d%f", &a, &b);
    // scanf("%f%f", &a, &b);
    // scanf("%lf%lf", &a, &b);
    // & = Address Operator
    printf("Sum = %d\n", a+b);
}