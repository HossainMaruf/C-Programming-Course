#include <stdio.h>
void main() {
    /**
     * SYNTAX
     * DataType VariableName;
     */
    int number, number10, num10ber, number_, num_ber, _number, $number, num$ber, number$;
    /**
     * Name will contain only letter(A-Z, a-z), digit(0-9), underscore(_) and dollar($)
     * Don't start with digit
     * Don't declare more than 1 times
     * Don't use keywords/reserved words (32 keywords)
     * int, float, double, char, void, if, else, for,  while, do, struct, union, typedef
     * case, switch, break, continue, volatile, extern
     * Don't use main
     */
    int Number; // case sensitive language
    int main; // don't use
}